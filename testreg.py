from cash import CashRegister

register1 = CashRegister()

user_input = int(input("How many items do you have?"))

for i in range (0, user_input):
    a - register1.additem(int(input("Enter the price of one item. ")))

register.giveChange(int(input("Enter the amount paid. "))
